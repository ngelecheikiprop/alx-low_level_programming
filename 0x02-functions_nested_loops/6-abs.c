#include "main.h"
/**
 * _abs - findds the absolute of a number
 *@: give the number
 *
 *Return: returns the absolute number
 */
int _abs(int)
{
	if (int < 0)
		return (-int);
	else
		return (int);
}
