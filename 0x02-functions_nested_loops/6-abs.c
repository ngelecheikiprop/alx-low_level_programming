#include "main.h"
/**
 * _abs - findds the absolute of a number
 *@num: give the number
 *
 *Return: returns the absolute number
 */
int _abs(int num)
{
	if (num < 0)
		return (-num);
	else
		return (num);
}
